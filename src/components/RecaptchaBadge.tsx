import { memo } from "react";

export const RecaptchaBadge = memo(() => {
  return (
    <div className="recaptcha-container">
      {/* Google reCAPTCHA badge will appear here automatically */}
    </div>
  );
});
